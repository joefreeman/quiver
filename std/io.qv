// Standard library I/O module
// Provides convenient wrappers around the print and println builtins

[
  print: #(int | bin) { x => x ~> <print>! },
  println: #(int | bin) { x => x ~> <println>! }
]
