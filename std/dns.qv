'ip = IPv4['bin] | IPv6['bin]

[
  resolve: #Str['bin] {
    .0 __dns_resolve__ [~, #\DnsResolver {
      $ __dns_next__ {
        | <> => { $ __dns_close__, [] }
        | =b, b __binary_length__ { =4 => IPv4[b] | =16 => IPv6[b] } [~, $]
      }
    }] %iter.unfold
  }
]
